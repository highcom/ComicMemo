<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/camera_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/sirahanairo"
    android:orientation="vertical">

    <!-- ======= カメラ領域 (上半分) ======= -->
    <FrameLayout
        android:id="@+id/camera_area"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="3">

        <!-- カメラプレビュー（バナー広告と同居時、SurfaceView 実装だと描画合成が崩れることがあるため COMPATIBLE を指定） -->
        <androidx.camera.view.PreviewView
            android:id="@+id/previewView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="#000"
            app:implementationMode="compatible" />

        <!-- 中央バーコード枠線 (単純な線を4本組み合わせ) -->
        <View
            android:id="@+id/barcode_frame"
            android:layout_width="250dp"
            android:layout_height="150dp"
            android:layout_gravity="center"
            android:background="@drawable/barcode_frame_shape" />

        <!-- 読み取り中プログレスバー (非表示→解析中のみ表示) -->
        <ProgressBar
            android:id="@+id/progress_bar"
            style="?android:attr/progressBarStyleLarge"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_gravity="center"
            android:visibility="gone" />
    </FrameLayout>

    <!-- 結果・手入力エリア（内容が多い場合・IME 表示時に縦スクロール） -->
    <androidx.core.widget.NestedScrollView
        android:id="@+id/barcode_result_scroll"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="4"
        android:clipToPadding="false"
        android:fillViewport="true"
        android:overScrollMode="ifContentScrolls">

        <androidx.cardview.widget.CardView
            android:id="@+id/detail_content"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/geppaku"
            app:cardCornerRadius="6dp"
            app:cardUseCompatPadding="true">

            <LinearLayout
                android:id="@+id/result_area"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="16dp">

                <TextView
                    android:id="@+id/search_limit_message"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:paddingBottom="8dp"
                    android:textColor="@color/red"
                    android:visibility="gone" />

                <Button
                    android:id="@+id/go_to_premium_button"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_gravity="center"
                    android:layout_marginBottom="8dp"
                    android:background="@drawable/round_button"
                    android:text="@string/go_to_premium_plan"
                    android:textColor="@color/white"
                    android:visibility="gone" />

                <TextView
                    android:id="@+id/barcode_result"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:paddingBottom="12dp"
                    android:text="@string/barcode_result"
                    android:textSize="16sp" />

                <EditText
                    android:id="@+id/manual_input"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="10dp"
                    android:hint="@string/barcode_input_isbn"
                    android:importantForAutofill="no"
                    android:inputType="number"
                    android:padding="8dp" />

                <Button
                    android:id="@+id/search_button"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="12dp"
                    android:layout_marginBottom="50dp"
                    android:background="@drawable/round_button"
                    android:text="@string/barcode_search_isbn"
                    android:textColor="@color/white" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>
    </androidx.core.widget.NestedScrollView>
</LinearLayout>
